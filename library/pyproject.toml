# pyproject.toml
# python -m pip install --editable library/  # to insall decencies
[build-system]
requires = ["setuptools>64.0.0","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tic-tac-toe"
version = "1.0.0"
